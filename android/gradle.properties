org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# Kotlin incremental compilation is disabled because it cannot close its caches
# when the project path contains a space, which this one does
# ("D:\Flutter Data\Projects\AL Mafia"). The failure is a hard build error in
# :audioplayers_android:compileReleaseKotlin, not a warning, and it reproduces
# from a clean build directory. The cost is a slower Kotlin compile for the two
# plugins that have Kotlin sources at all; the project's own code is Dart.
kotlin.incremental=false
